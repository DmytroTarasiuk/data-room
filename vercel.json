{
  "buildCommand": "npm run db:generate && npm run build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/rooms/:path*", "destination": "/" },
    { "source": "/share/:path*", "destination": "/" }
  ]
}
